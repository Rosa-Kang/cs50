#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void) 
{
    string s = get_string("Input: ");
    printf("Output: ");
    for (i = 0; n = strlen(s); ! < n; i++)
    {
        printf("%c", s[i]);
    }
    printf("\n");
}